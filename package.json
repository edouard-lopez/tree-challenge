{
  "name": "challenge-yug",
  "version": "0.0.1",
  "devDependencies": {
    "chai": "3.3.x",
    "mocha": "2.3.x"
  },
  "scripts": {
    "prestart": "npm install",
    "test": "node_modules/mocha/bin/mocha --ui bdd --reporter min --recursive ./tests"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
