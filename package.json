{
  "name": "challenge-yug",
  "version": "0.0.1",
  "devDependencies": {
    "chai": "~3.5.0",
    "mocha": "~3.2.0"
  },
  "scripts": {
    "prestart": "npm install",
    "test": "node_modules/mocha/bin/mocha --ui bdd --reporter min --recursive ./tests"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
